[project]
name = "riku"
version = "0.1.0"
description = "VCS especializado para diseño de chips"
requires-python = ">=3.11"
dependencies = [
    "pygit2>=1.14",
]

[project.scripts]
riku = "riku.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
